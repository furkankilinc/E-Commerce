server {
    listen 80;
    server_name user.fuira.com;

    resolver 127.0.0.11 valid=30s;

    location / {
        set $target_storefront http://storefront:5173;
        proxy_pass $target_storefront;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name admin.fuira.com;

    resolver 127.0.0.11 valid=30s;

    location / {
        set $target_admin http://admin:5174;
        proxy_pass $target_admin;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name merchant.fuira.com;

    resolver 127.0.0.11 valid=30s;

    location / {
        set $target_vendor http://vendorpanel:5175;
        proxy_pass $target_vendor;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name api.localhost;

    resolver 127.0.0.11 valid=30s;

    location / {
        set $target_backend http://storebackend:5000;
        proxy_pass $target_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

